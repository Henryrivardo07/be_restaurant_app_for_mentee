# Database (Neon pooler URL, SSL required)
DATABASE_URL="postgresql://<USERNAME>:<PASSWORD>@<HOST>/<DBNAME>?sslmode=require"

# JWT settings (untuk auth token kalau ada)
JWT_SECRET="secret-demo"
JWT_EXPIRES_IN="7d"

# App settings
PORT=8080
NODE_ENV=production